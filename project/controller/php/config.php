<?php
    $hostname = "localhost:3308";
    $username = "root";
    $password = "";
    $dbname = "tradechatapp";

    // Crear la conexión
    $conn = mysqli_connect($hostname, $username, $password, $dbname);

?>
